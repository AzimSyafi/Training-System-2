# Database Configuration for the app and helper scripts
# Copy this to your environment or .env file

# PostgreSQL connection string (psycopg3-friendly)
# NOTE: Keep this as postgresql:// (or postgres://). Do NOT use postgresql+psycopg:// here,
# because helper scripts call psycopg.connect() which doesn't accept SQLAlchemy dialect prefixes.
# The Flask app will automatically convert postgresql:// to postgresql+psycopg:// for SQLAlchemy.
DATABASE_URL=postgresql://postgres:7890@localhost:5432/Training_system

# Alternative format if needed (Heroku-style):
# DATABASE_URL=postgres://training_user:your_password_here@localhost:5432/security_training

